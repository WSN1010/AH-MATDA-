# IMPLEMENTATION-FRONTEND - 아주르 프론트엔드 구현 가이드

| 항목 | 값 |
|---|---|
| 담당 | 프론트엔드 담당자 1인 |
| 범위 | `src/Ajure.Web/` 전체 |
| 기준 문서 | [UX-SPEC.md](UX-SPEC.md)(구조·기능), [PRD.md](PRD.md) §7, TRD §9(API) |
| 공통 규칙 | [AGENTS.md](AGENTS.md) 필수 준수 (ponytail, 스킬 자동 적용 포함) |

이 문서는 **핵심 기능만** 명시한다. 시각 디자인은 여기서 확정하지 않으며, §5의 디자인 결정 프로토콜에 따라 **사용자에게 질문해서 확정**한다.

## 1. 기술 베이스

- React + TypeScript + Vite SPA (TRD §3.1)
- 의존성 최소주의: 라우터, Markdown 렌더러 수준 외 추가 라이브러리는 필요가 증명될 때만
- 상태 관리는 React 내장 기능부터 시작한다 (전역 상태 라이브러리 선제 도입 금지)
- API 호출은 얇은 fetch 래퍼 하나로 통일한다

## 2. 핵심 기능 (이것만 구현한다)

| ID | 기능 | 관련 FR | 화면 |
|---|---|---|---|
| F-01 | 프로젝트 목록·생성, 아이디어 입력 | FR-001 | `/`, `/projects`, `/projects/new` |
| F-02 | 대상 코딩 에이전트 다중 선택 + 출력 경로 표시 | FR-002 | 생성 플로우 내 |
| F-03 | 결정 인터뷰: Critical/Important/Defaultable 질문, 추천값 승인·수정 | FR-003 | `/projects/{id}/decisions` |
| F-04 | 생성 진행 화면: SSE 단계 표시, 재연결 이어보기, 실패 시 재시도 | FR-014 | `/projects/{id}/run/{jobId}` |
| F-05 | 문서 워크벤치: 4종 문서 탭, Markdown 미리보기/편집, Stale 배지 | FR-011 | `/projects/{id}/workspace` |
| F-06 | 검증 리포트: 점수, Hard Gate 통과 여부, Finding 목록, 회귀 결과 | FR-010 | 워크벤치 내 Validation 패널 |
| F-07 | Export: **Ready 상태에서만 ZIP 다운로드** 버튼 활성화, 포함 파일 미리보기 | FR-013 | 워크벤치 내 |
| F-08 | 로컬 모델 공급자 설정: 구성 상태, API 키/모델 저장·제거, 운영자 관리 잠금 | FR-017 | `/settings/providers` |

### 기능 공통 요구

- 모든 화면에 loading / empty / error 상태를 구현한다 (스켈레톤 또는 명시 메시지)
- F-04의 SSE는 `Last-Event-ID`를 저장해 새로고침 후 이어본다 (TRD §9 SSE 규칙)
- F-07의 결과물은 항상 **ZIP 파일 1개**다. 개별 md 다운로드는 MVP 범위가 아니다
- 위 표에 없는 기능(다크모드 토글, 온보딩 투어, 모델 공급자 외 설정 페이지 등)은 만들지 않는다

## 3. API 연동

- 계약은 [TRD.md](TRD.md) §9 표가 유일한 기준이다. 임의로 엔드포인트를 추측하지 않는다
- 오류는 Problem Details(`code`, `message`, `retryable`) 기준으로 공통 처리한다
- 백엔드 준비 전에는 백엔드의 **Fake 모드**(`AJURE_FAKE_MODEL=true`, IMPLEMENTATION-BACKEND §B3)를 사용한다
- Fake 모드도 없을 때만 fetch 래퍼 뒤에 로컬 목 데이터를 둔다 (별도 목 서버 라이브러리 도입 금지)
- F-08 공급자 설정은 목 저장을 사용하지 않는다. localhost API에 연결할 수 없으면 실패 상태를 표시한다

## 4. 개발 순서

1. **W0**: Vite 스캐폴드 + 라우팅 + fetch 래퍼 + Aspire AppHost 등록(백엔드와 협의)
2. **W1**: F-01, F-02 (프로젝트 생성 플로우)
3. **W2**: F-04 (SSE 진행 화면) — 백엔드 B3와 맞물리는 첫 통합 지점
4. **W3**: F-03 (결정 인터뷰)
5. **W4**: F-05 (문서 워크벤치)
6. **W5**: F-06, F-07 (검증 리포트 + ZIP export)
7. **W6**: 접근성·반응형 마감 (§6)
8. **W7**: F-08 (localhost 전용 모델 공급자 설정)

각 단계 완료 시 `npm run build`와 타입 체크 통과를 확인하고 푸시한다.

## 5. 디자인 결정 프로토콜 (필수)

시각 디자인은 이 문서나 구현 에이전트가 임의로 정하지 않는다.

### 규칙

1. **스타일링 작업을 시작하기 전에**, 구현 에이전트는 아래 질문을 사용자에게 물어 답을 받는다.
2. 사용자가 답하지 않은 항목만 [UX-SPEC.md](UX-SPEC.md) §2의 제안 기본값을 사용한다.
3. 확정된 답은 UX-SPEC §2에 반영해 커밋한다. 이후 이 결정이 디자인의 기준선이 된다.
4. 기준선이 생긴 뒤 새로운 디자인 판단이 필요해지면 다시 질문한다. 추측으로 채우지 않는다.

### 디자인 인터뷰 질문 목록

| # | 질문 | 선택지 예시 |
|---|---|---|
| Q1 | 전체 무드는? | 전문 도구형 / 친근한 프로덕트형 / 개발자 터미널형 |
| Q2 | 기본 테마는? | 라이트 / 다크 / 시스템 따름 |
| Q3 | 주 색상 방향은? | 블루 계열(아주르 연상) / 뉴트럴+포인트 1색 / 직접 지정 |
| Q4 | 밀도는? | 여백 넉넉한 문서형 / 콤팩트한 대시보드형 |
| Q5 | 타이포 방향은? | 시스템 폰트 / Sans 지정 / 본문·코드 폰트 분리 |
| Q6 | 참고하고 싶은 제품이 있는가? | 자유 응답 |
| Q7 | 브랜드 표기는? | 아주르 / AJURE / 병기 |
| Q8 | 점수·게이트 시각화 취향은? | 숫자 중심 / 게이지·차트 중심 / 체크리스트 중심 |

### 변하지 않는 것 (질문 대상 아님)

- 화면 구조, 정보 구조, 사용자 흐름 (UX-SPEC §3 이후)
- 접근성·반응형 기준 (§6)
- 핵심 기능 범위 (§2)

## 6. 접근성·반응형 최소 기준

- 키보드만으로 전체 플로우 완주 가능, 포커스 표시 유지 (NFR-004, AC-018)
- 인터랙티브 요소에 접근 가능한 이름 제공
- 텍스트 대비 WCAG AA
- 지원 폭: 360px(모바일) ~ 1440px(데스크톱), 워크벤치는 모바일에서 탭 전환형으로 축소
- SSE 진행 상태는 스크린리더에 라이브 리전으로 전달

## 7. 검증 의무

푸시 전 최소한 다음을 확인한다.

1. `npm run build` 성공 + TypeScript 오류 0
2. 변경 화면을 실제 브라우저에서 360px/1440px 두 폭으로 확인
3. 해당 화면의 loading/empty/error 상태 렌더 확인

## 8. 금지 사항

- 백엔드 프로젝트(`src/Ajure.Web` 외부) 수정 — 필요하면 백엔드 담당자에게 요청
- §2 표에 없는 기능 추가
- 사용자에게 묻지 않은 시각 디자인 확정
- API 계약(TRD §9)에 없는 요청 경로 사용

## 9. 완료 정의

- [ ] F-01~F-08 전부 Fake 모드 E2E로 동작
- [ ] 디자인 인터뷰 완료, UX-SPEC §2에 확정값 반영
- [ ] §6 접근성·반응형 기준 통과
- [ ] Ready 프로젝트에서 ZIP 다운로드 성공
