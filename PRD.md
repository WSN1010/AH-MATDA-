# PRD - 아주르

## 1. 문서 정보

| 항목 | 값 |
|---|---|
| 제품 | 아주르 (AJURE) |
| 유형 | 웹 기반 명세 엔지니어링 에이전트 |
| 상태 | 기획 확정 초안 |
| 핵심 목표 | 외부 코딩 에이전트의 원샷 구현 의도 충족률 향상 |
| 구현 여부 | 본 문서는 구현 계획이며 제품 코드를 포함하지 않음 |

## 2. 제품 비전

아주르는 사용자의 아이디어를 받아 코드를 쓰는 대신, 선택한 코딩 에이전트가 한 번의 구현 요청으로 최대한 정확하게 실행할 수 있는 명세 번들을 만든다. 작성, 교차 검토, 회귀 검사, 보정 과정을 반복해 문서가 구현 가능한 상태인지 근거와 함께 판정한다.

## 3. 목표

- 아이디어를 `IDEATION.md`, `PRD.md`, `TRD.md`로 구조화한다.
- 선택한 코딩 에이전트의 네이티브 지침 파일을 생성한다.
- 문서 간 요구사항 추적률 100%를 달성한다.
- 승인 버전 대비 의도하지 않은 요구사항 손실을 차단한다.
- Ready 번들의 One-Shot Readiness Score를 90점 이상으로 만든다.
- 실제 외부 구현 벤치마크에서 첫 시도 수용 기준 충족률 중앙값 90% 이상을 목표로 한다.

## 4. 비목표

- 아주르가 제품 소스 코드를 생성하거나 수정하지 않는다.
- 사용자의 로컬 파일 시스템이나 GitHub 저장소에서 구현 명령을 실행하지 않는다.
- 배포, 마이그레이션, 결제 같은 작업을 대신 실행하지 않는다.
- 모든 코딩 에이전트가 반드시 동일한 결과를 낸다고 보장하지 않는다.
- 일반 문서 편집기나 프로젝트 관리 도구 전체를 대체하지 않는다.

## 5. 사용자

### P-01 해커톤 빌더
- 짧은 시간 안에 기획과 기술 결정을 끝내야 한다.
- 전문 PM/디자이너/아키텍트가 없는 경우가 많다.
- 문서를 코딩 에이전트에 바로 전달하고 싶다.

### P-02 AI 네이티브 개발자
- Claude Code, Copilot, Codex, Gemini CLI 등을 사용한다.
- 반복 프롬프트와 재작업을 줄이고 싶다.
- 도구별 지침 파일을 직접 관리하기 어렵다.

### P-03 소규모 제품팀
- 팀의 요구사항을 하나의 기준으로 맞추고 싶다.
- 변경 시 어떤 요구사항이 사라졌는지 확인하고 싶다.

## 6. 핵심 사용자 여정

### J-01 새 명세 만들기

1. 사용자가 새 프로젝트를 만든다.
2. 자유 형식으로 아이디어를 입력하거나 기존 메모를 붙여 넣는다.
3. 구현을 맡길 대상 코딩 에이전트를 하나 이상 선택한다.
4. 아주르가 결과에 큰 영향을 주는 질문만 제시한다.
5. 사용자가 추천값을 승인하거나 답을 수정한다.
6. 아주르가 공통 문서와 대상 지침 파일을 생성한다.
7. 검증 루프가 누락, 모순, 모호성, 추적성, 회귀를 검사한다.
8. 자동 해결할 수 없는 결정만 사용자에게 돌려보낸다.
9. 90점 및 필수 게이트 통과 후 Ready 번들을 다운로드한다.

### J-02 문서 변경 후 재검증

1. 사용자가 문서의 특정 요구사항을 편집한다.
2. 아주르가 영향을 받는 요구사항과 문서를 표시한다.
3. 사용자가 변경 의도를 승인한다.
4. 관련 문서와 대상 지침 파일만 다시 동기화한다.
5. 이전 승인 버전과 의미 회귀를 검사한다.
6. 새 버전을 Ready 또는 Needs Decision 상태로 판정한다.

### J-03 여러 코딩 에이전트용 번들 만들기

1. 사용자가 Claude Code와 Copilot처럼 여러 대상을 선택한다.
2. 아주르는 공통 문서 3개를 한 번만 생성한다.
3. `CLAUDE.md`, `AGENTS.md` 등 대상별 실행 계약을 각각 생성한다.
4. 파일 경로가 겹치면 호환 규칙으로 병합하고 충돌을 보고한다.

## 7. 기능 요구사항

### FR-001 프로젝트 생성

- 사용자는 프로젝트 이름과 아이디어 설명으로 프로젝트를 생성할 수 있어야 한다.
- 시스템은 각 프로젝트에 고유 ID와 첫 명세 버전을 부여해야 한다.
- 입력은 한국어와 영어를 지원해야 한다.

### FR-002 대상 코딩 에이전트 선택

- 사용자는 지원 목록에서 하나 이상의 대상 도구를 선택할 수 있어야 한다.
- “문서를 만드는 생성 모델”과 “문서를 전달할 대상 코딩 에이전트”를 UI에서 명확히 구분해야 한다.
- 대상별 실제 출력 경로와 지원 수준을 선택 화면에서 보여 줘야 한다.

### FR-003 결정 중심 인터뷰

- 시스템은 원본 아이디어에서 구현 결과를 바꾸는 미결정 사항을 추출해야 한다.
- 질문은 Critical, Important, Defaultable로 분류해야 한다.
- Critical은 사용자의 명시적 답이 없으면 Ready 상태가 될 수 없다.
- Defaultable은 추천값과 근거를 제공하고 사용자가 바꿀 수 있어야 한다.
- 이미 답한 내용을 표현만 바꿔 반복 질문해서는 안 된다.

### FR-004 정규화된 명세 모델

- 모든 문서는 하나의 내부 `ProjectSpec`에서 파생돼야 한다.
- 목표, 요구사항, 수용 기준, 기술 결정, 리스크에는 안정적인 ID가 있어야 한다.
- 문서별 독립 생성으로 서로 다른 사실을 만들면 안 된다.

### FR-005 공통 문서 생성

- 시스템은 항상 `IDEATION.md`, `PRD.md`, `TRD.md`를 생성해야 한다.
- 문서는 [DOCUMENT-SPEC.md](DOCUMENT-SPEC.md)의 구조와 각 문서의 고유 목적을 유지해야 한다.
- 구현할 수 없는 추상 표현에는 검증 가능한 기준 또는 명시적 결정이 따라야 한다.
- 문서에는 코드 블록 형태의 제품 구현을 포함하지 않아야 한다. API 스키마, 디렉터리 구조, 의사 계약은 허용한다.

### FR-006 대상 지침 파일 생성

- 선택한 각 코딩 에이전트의 네이티브 파일명과 경로를 사용해야 한다.
- 대상 파일은 검증된 공통 문서의 내용을 새로 해석하지 않고 실행 순서로 변환해야 한다.
- 대상 파일은 최소한 문서 읽기 순서, 우선순위, 구현 범위, 금지 사항, 완료 기준, 검증 명령 원칙을 포함해야 한다.
- 최신 명세 버전과 일치하지 않는 대상 파일은 Stale 상태로 표시해야 한다.

### FR-007 문서 교차 검증

- 모든 기능 요구사항은 하나 이상의 수용 기준과 연결돼야 한다.
- 모든 기술 구성요소는 하나 이상의 제품 요구사항 또는 비기능 요구사항에 근거해야 한다.
- PRD와 TRD의 사용자 흐름, API, 데이터, 보안, 배포 조건이 모순되지 않아야 한다.
- 구현에 영향을 주는 정의되지 않은 용어를 찾아야 한다.

### FR-008 다중 평가 및 자동 보정

- 작성자와 평가자는 서로 다른 Agent Framework 역할과 세션을 사용해야 한다.
- 결정적 검사를 통과한 뒤 제품, 기술, UX, 실행 가능성 평가를 수행해야 한다.
- 실패 항목은 근거, 영향, 관련 ID, 권장 수정안을 포함해야 한다.
- 사용자 결정이 필요 없는 항목은 최대 3회까지 자동 보정할 수 있어야 한다.
- 3회 후에도 실패하거나 동일 문제가 반복되면 사용자에게 명확한 선택지를 제시해야 한다.

### FR-009 회귀 검증

- 승인된 명세 버전을 기준선으로 저장해야 한다.
- 새 버전에서 삭제, 약화, 충돌, 미연결된 요구사항을 탐지해야 한다.
- 텍스트가 바뀌어도 의미가 보존되면 회귀로 판정하지 않아야 한다.
- 의도적 삭제는 사용자 승인과 사유를 기록해야 한다.
- 회귀 실패 상태에서는 Ready 번들을 생성할 수 없어야 한다.

### FR-010 One-Shot Readiness

- 시스템은 [EVALUATION.md](EVALUATION.md)의 공식 산식으로 0~100 점수를 계산해야 한다.
- 점수와 별도로 Hard Gate 통과 여부를 표시해야 한다.
- 90점 이상이어도 Hard Gate가 하나라도 실패하면 Ready가 아니어야 한다.
- 점수의 세부 항목과 근거를 사용자가 확인할 수 있어야 한다.

### FR-011 문서 편집과 영향 분석

- 사용자는 웹에서 각 문서를 미리 보고 편집할 수 있어야 한다.
- 편집 전후 차이와 영향을 받는 요구사항을 표시해야 한다.
- 수동 편집 후 관련 산출물을 Stale로 표시하고 재검증을 요구해야 한다.
- 사용자의 원문을 자동 보정이 덮어쓸 때 변경점을 확인할 수 있어야 한다.

### FR-012 버전 관리

- Draft, Validating, Needs Decision, Ready, Superseded 상태를 지원해야 한다.
- 각 버전은 입력, 답변, 생성 설정, 파일 해시, 평가 결과를 보존해야 한다.
- 사용자는 이전 버전을 열람하고 비교할 수 있어야 한다.
- 승인 버전은 수정하지 않고 새 버전을 만들어야 한다.

### FR-013 내보내기

- Ready 상태에서만 구현용 ZIP을 내보낼 수 있어야 한다.
- ZIP은 공통 문서 3개와 선택 대상의 지침 파일만 기본 포함해야 한다.
- 평가 리포트는 별도 선택 항목으로 제공해 구현 에이전트의 컨텍스트를 불필요하게 늘리지 않아야 한다.
- 경로를 가진 규칙 파일은 ZIP 내부에서도 정확한 디렉터리에 배치해야 한다.

### FR-014 실시간 진행 상태

- 작성, 검사, 평가, 보정 단계의 진행 상태를 제공해야 한다.
- 장시간 작업은 페이지를 새로 열어도 이어서 확인할 수 있어야 한다.
- 실패 시 실패 단계와 재시도 가능 여부를 보여 줘야 한다.

### FR-015 프로젝트 삭제

- 사용자는 프로젝트와 관련 산출물을 삭제할 수 있어야 한다.
- 삭제는 확인 절차를 거쳐야 하며 보존 정책에 따른 실제 삭제 상태를 표시해야 한다.

### FR-016 필수 기술 플랫폼

- 작성, 평가, 회귀, 보정 워크플로는 Microsoft Agent Framework에서 실행돼야 한다.
- 최소 Spec Architect와 독립 Reviewer 한 단계는 GitHub Copilot SDK 세션을 성공적으로 사용해야 한다.
- Copilot SDK 필수 단계가 실패하면 다른 제공자로 조용히 대체해 Ready 결과를 만들면 안 된다.
- 모델 호출은 `IChatClient` 경계 뒤에 구성해야 한다.
- 로컬 구성과 Azure 배포는 .NET Aspire AppHost를 기준으로 해야 한다.

## 8. 비기능 요구사항

### NFR-001 성능
- 첫 질문 세트는 요청 후 15초 이내를 목표로 한다.
- 문서 초안 4종은 일반 프로젝트 기준 120초 이내를 목표로 한다.
- 검증 진행 이벤트는 3초 이상 정지해 보이지 않아야 한다.

### NFR-002 신뢰성
- 모든 생성 작업은 재시도 가능한 Job으로 저장해야 한다.
- 동일 Job의 중복 실행이 중복 버전을 만들지 않도록 멱등성을 보장해야 한다.
- 부분 실패 시 완료된 산출물과 실패 단계를 보존해야 한다.

### NFR-003 보안
- GitHub 토큰과 모델 자격증명은 Azure Key Vault에 저장해야 한다.
- 사용자 문서를 모델 학습에 사용한다고 가정해서는 안 된다.
- 로그에 원문 전체, 토큰, 개인정보를 기록해서는 안 된다.
- 데이터 전송과 저장은 암호화해야 한다.

### NFR-004 접근성
- WCAG 2.2 AA를 목표로 한다.
- 키보드만으로 프로젝트 생성, 답변, 검토, 내보내기가 가능해야 한다.
- 점수와 상태를 색상만으로 전달해서는 안 된다.

### NFR-005 관측성
- Project ID, Spec Version, Validation Run ID를 분산 추적에 포함해야 한다.
- 단계별 지연, 토큰 사용량, 실패율, 보정 횟수를 수집해야 한다.
- 사용자 문서 본문은 기본 Telemetry에서 제외해야 한다.

### NFR-006 이식성
- AI 제공자 호출은 `IChatClient` 경계 뒤에 있어야 한다.
- 대상 도구 파일 형식은 코드 변경 없이 템플릿/레지스트리 버전으로 확장 가능해야 한다.

### NFR-007 배포
- 로컬 구성과 Azure 구성을 .NET Aspire AppHost에서 선언해야 한다.
- Azure 배포는 Aspire와 Azure Developer CLI 기반 경로를 사용해야 한다.

## 9. 비즈니스 규칙

1. `Ready = Score >= 90 AND all hard gates pass`.
2. Critical 질문이 미해결이면 원점수와 관계없이 Ready가 될 수 없다.
3. 기능 요구사항과 수용 기준의 추적률은 100%여야 한다.
4. 대상 지침 파일의 명세 버전이 공통 문서와 다르면 내보낼 수 없다.
5. 승인된 요구사항 삭제는 사용자의 명시적 승인 없이는 허용하지 않는다.
6. 아주르가 생성하는 파일은 코드 구현을 포함하지 않는다.
7. 코드 생성 요청이 들어오면 제품은 구현 대신 명세에 필요한 정보를 추출한다.

## 10. 정보 구조

| 경로 | 목적 |
|---|---|
| `/` | 제품 설명과 새 프로젝트 시작 |
| `/projects` | 프로젝트 목록 |
| `/projects/new` | 아이디어 입력과 대상 선택 |
| `/projects/{id}/questions` | 결정 질문 응답 |
| `/projects/{id}/workbench` | 문서, 점수, 이슈 통합 작업 공간 |
| `/projects/{id}/history` | 버전 및 회귀 비교 |

상세 화면 규격은 [UX-SPEC.md](UX-SPEC.md)를 따른다.

## 11. 분석 이벤트

| 이벤트 | 필수 속성 |
|---|---|
| `project_created` | locale, input_length |
| `target_selected` | target_ids |
| `questions_completed` | critical_count, defaults_accepted |
| `generation_started` | spec_version, target_count |
| `validation_completed` | score, hard_gate_failures, iteration_count |
| `decision_requested` | category, related_requirement_ids |
| `bundle_exported` | target_ids, score, elapsed_seconds |
| `version_regression_failed` | regression_type, affected_ids |

문서 원문과 사용자 비밀은 이벤트 속성에 포함하지 않는다.

## 12. 수용 기준

### AC-001 Claude 번들

**Given** 사용자가 Claude Code를 대상으로 선택하고 필수 질문에 답했을 때
**When** 명세 생성과 검증이 완료되면
**Then** ZIP에는 `IDEATION.md`, `PRD.md`, `TRD.md`, `CLAUDE.md`가 있어야 하며 모든 파일의 명세 버전이 같아야 한다.

### AC-002 Copilot/Codex 번들

**Given** 사용자가 Copilot 또는 Codex를 선택했을 때
**When** Ready 번들을 내보내면
**Then** 루트 `AGENTS.md`가 생성되고 세 공통 문서의 읽기 순서와 완료 조건을 포함해야 한다.

### AC-003 다중 대상

**Given** 사용자가 Claude Code, Gemini CLI, Cursor를 함께 선택했을 때
**When** 번들을 내보내면
**Then** `CLAUDE.md`, `GEMINI.md`, `.cursor/rules/ajure.mdc`가 정확한 경로에 함께 존재해야 한다.

### AC-004 추적성 게이트

**Given** `FR-012`에 연결된 수용 기준이 없을 때
**When** 검증을 실행하면
**Then** Ready 판정은 실패하고 관련 ID와 수정 제안이 표시돼야 한다.

### AC-005 모순 게이트

**Given** PRD는 이메일 로그인을 요구하지만 TRD는 인증 없음으로 정의할 때
**When** 교차 검증을 실행하면
**Then** 모순이 Critical로 보고되고 자동 내보내기가 차단돼야 한다.

### AC-006 회귀 보호

**Given** 승인 버전에 결제 실패 흐름이 존재할 때
**When** 새 버전에서 해당 흐름이 사라지면
**Then** 사용자가 의도적 삭제를 승인하기 전까지 회귀 실패여야 한다.

### AC-007 수동 편집 무효화

**Given** Ready 상태의 PRD를 사용자가 수정했을 때
**When** 변경을 저장하면
**Then** 관련 TRD와 대상 지침 파일은 Stale로 표시되고 Ready 상태가 해제돼야 한다.

### AC-008 코드 작성 방지

**Given** 사용자가 “이 앱 코드를 작성해 줘”라고 입력했을 때
**When** 아주르가 요청을 처리하면
**Then** 코드를 출력하지 않고 해당 요청에서 요구사항과 기술 결정을 추출해 명세 생성 흐름으로 전환해야 한다.

### AC-009 Ready 판정

**Given** 점수가 92점이지만 Critical 질문이 하나 남아 있을 때
**When** 검증 결과를 표시하면
**Then** 상태는 Ready가 아닌 Needs Decision이어야 한다.

### AC-010 복구

**Given** 두 번째 평가 단계에서 일시적 모델 오류가 발생했을 때
**When** Job이 재시도되면
**Then** 이미 완료된 결정적 검사를 반복 저장하지 않고 동일 Validation Run을 이어가야 한다.

### AC-011 프로젝트 생성

**Given** 유효한 프로젝트 이름과 아이디어를 입력했을 때
**When** 프로젝트 생성을 완료하면
**Then** 고유 Project ID와 불변의 첫 Spec Version이 생성되고 프로젝트 목록에서 다시 열 수 있어야 한다.

### AC-012 결정 중심 인터뷰

**Given** 원본 아이디어에 인증 방식과 데이터 보존 기간이 정해지지 않았을 때
**When** 아이디어 분석이 끝나면
**Then** 결과에 영향을 주는 질문은 심각도, 필요 이유, 추천값, 선택별 구현 영향을 포함해야 하며 이미 답한 질문을 반복하지 않아야 한다.

### AC-013 단일 ProjectSpec

**Given** 하나의 요구사항이 PRD와 TRD에 모두 표현될 때
**When** 산출물 검증을 실행하면
**Then** 두 문서는 동일 Requirement ID와 의미를 참조해야 하고 독립적으로 충돌하는 사실을 포함해서는 안 된다.

### AC-014 불변 버전

**Given** Ready 버전이 존재할 때
**When** 사용자가 요구사항을 변경하면
**Then** 기존 버전은 변경되지 않고 기준 버전을 참조하는 새 후보 버전이 생성돼야 하며 History에서 둘을 비교할 수 있어야 한다.

### AC-015 프로젝트 삭제

**Given** 사용자가 프로젝트 삭제를 요청했을 때
**When** 경고를 확인하고 삭제를 확정하면
**Then** 프로젝트는 즉시 접근 불가 상태가 되고 UI에 보존 정책에 따른 영구 삭제 예정 또는 완료 상태가 표시돼야 한다.

### AC-016 성능 및 진행 이벤트

**Given** 일반 규모의 프로젝트 생성 Job이 실행 중일 때
**When** 사용자가 Workbench를 열거나 새로 고치면
**Then** 3초 이내에 최신 진행 상태가 표시되고 마지막 확인 지점 이후의 이벤트를 이어서 볼 수 있어야 한다.

### AC-017 비밀 및 데이터 보호

**Given** 사용자 입력 또는 모델 오류에 토큰처럼 보이는 문자열이 포함될 때
**When** 로그, Telemetry, Export를 생성하면
**Then** 실제 자격증명이 기록되지 않아야 하고 사용자 프로젝트 데이터는 소유자 범위 밖에서 조회되지 않아야 한다.

### AC-018 접근성

**Given** 사용자가 포인팅 장치 없이 웹 앱을 사용할 때
**When** 프로젝트 생성부터 Ready ZIP 내보내기까지 진행하면
**Then** 모든 필수 행동을 키보드로 완료할 수 있고 상태와 오류를 색상 외의 텍스트로 식별할 수 있어야 한다.

### AC-019 관측성

**Given** 생성 Job이 Worker에서 실패했을 때
**When** 운영자가 해당 Correlation ID를 조회하면
**Then** API부터 실패 단계까지 Trace를 찾을 수 있어야 하며 사용자 문서 본문과 비밀은 기본 Telemetry에 없어야 한다.

### AC-020 이식성과 Aspire 배포

**Given** 동일 애플리케이션을 로컬과 Azure에서 실행할 때
**When** Aspire AppHost와 `azd` 배포 경로를 사용하면
**Then** 서비스 참조와 Health Check가 유지되고 모델 경계는 `IChatClient` 계약을 통해 교체 가능해야 한다.

### AC-021 필수 Agent Framework와 Copilot SDK

**Given** 생성/검증 Job이 Ready 후보에 도달했을 때
**When** 실행 기록을 검사하면
**Then** Microsoft Agent Framework 워크플로 안에서 Spec Architect와 독립 Reviewer가 실행됐고 최소 두 단계가 Copilot SDK 세션을 성공적으로 사용했음을 확인할 수 있어야 한다.

## 13. 요구사항 추적 매트릭스

| 요구사항 | 수용 기준 |
|---|---|
| FR-001 | AC-011 |
| FR-002 | AC-003 |
| FR-003 | AC-009, AC-012 |
| FR-004 | AC-013 |
| FR-005 | AC-001, AC-008, AC-013 |
| FR-006 | AC-001, AC-002, AC-003 |
| FR-007 | AC-004, AC-005 |
| FR-008 | AC-010 |
| FR-009 | AC-006 |
| FR-010 | AC-004, AC-009 |
| FR-011 | AC-007 |
| FR-012 | AC-006, AC-014 |
| FR-013 | AC-001, AC-002, AC-003 |
| FR-014 | AC-010, AC-016 |
| FR-015 | AC-015 |
| FR-016 | AC-020, AC-021 |
| NFR-001 | AC-016 |
| NFR-002 | AC-010, AC-014, AC-016 |
| NFR-003 | AC-017 |
| NFR-004 | AC-018 |
| NFR-005 | AC-019 |
| NFR-006 | AC-003, AC-020 |
| NFR-007 | AC-020 |

## 14. MVP 출시 기준

- Claude Code, Copilot, Codex, Gemini CLI 네이티브 파일 생성
- Cursor, Devin Desktop/Windsurf, Cline, Amazon Q 프로젝트 공통 규칙 파일 생성
- 공통 문서 3개 생성
- 100% 요구사항 추적 게이트
- 모호성/모순/구현 가능성 평가
- 버전 스냅샷과 회귀 비교
- 점수 및 근거 대시보드
- ZIP 내보내기
- Aspire 기반 Azure 배포
- 최소 20개 벤치마크 브리프로 점수와 실제 결과의 상관 검증

## 15. 후속 범위

- 대상 도구별 디렉터리/파일 패턴 기반 고급 조건부 규칙 생성
- GitHub 저장소 구조 읽기 기반 Brownfield 명세
- 팀 템플릿 및 승인 워크플로
- 실제 외부 코딩 에이전트 결과 업로드 후 사후 평가
- 조직별 품질 기준과 벤치마크
